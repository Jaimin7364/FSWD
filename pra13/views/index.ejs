<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tax Income Calculator</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        
        .form-container {
            background: white;
            border-radius: 15px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            padding: 40px;
            max-width: 600px;
            width: 100%;
        }
        
        .header {
            text-align: center;
            margin-bottom: 40px;
        }
        
        .header h1 {
            color: #2c3e50;
            font-size: 2.5em;
            margin-bottom: 10px;
            font-weight: 700;
        }
        
        .header .subtitle {
            color: #7f8c8d;
            font-size: 1.1em;
            font-weight: 400;
        }
        
        .form-group {
            margin-bottom: 25px;
        }
        
        .form-group label {
            display: block;
            font-weight: 600;
            color: #34495e;
            margin-bottom: 8px;
            font-size: 1.1em;
        }
        
        .form-group input[type="number"] {
            width: 100%;
            padding: 15px 20px;
            border: 2px solid #e9ecef;
            border-radius: 10px;
            font-size: 1.1em;
            transition: all 0.3s ease;
            background: #f8f9fa;
        }
        
        .form-group input[type="number"]:focus {
            outline: none;
            border-color: #667eea;
            background: white;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }
        
        .form-group input[type="number"]:invalid {
            border-color: #e74c3c;
        }
        
        .input-note {
            font-size: 0.9em;
            color: #6c757d;
            margin-top: 5px;
            font-style: italic;
        }
        
        .submit-btn {
            width: 100%;
            padding: 18px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 1.2em;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            margin-top: 20px;
        }
        
        .submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.3);
        }
        
        .submit-btn:active {
            transform: translateY(0);
        }
        
        .icon {
            font-size: 1.2em;
            margin-right: 8px;
        }
        
        .disclaimer {
            background: #e8f4fd;
            border: 1px solid #bee5eb;
            border-radius: 8px;
            padding: 15px;
            margin-top: 25px;
            font-size: 0.9em;
            color: #0c5460;
        }
        
        .disclaimer-title {
            font-weight: 600;
            margin-bottom: 5px;
        }
        
        @media (max-width: 768px) {
            .form-container {
                padding: 30px 20px;
                margin: 10px;
            }
            
            .header h1 {
                font-size: 2em;
            }
        }
        
        .error-message {
            background: #f8d7da;
            color: #721c24;
            padding: 12px 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            border: 1px solid #f5c6cb;
            font-weight: 500;
        }
    </style>
</head>
<body>
    <div class="form-container">
        <div class="header">
            <h1><span class="icon">📊</span>Tax Income Calculator</h1>
            <p class="subtitle">Calculate your total income from multiple sources</p>
        </div>
        
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="error-message">
                <strong>⚠️ Error:</strong> <%= error %>
            </div>
        <% } %>
        
        <form action="/calculate-income" method="POST" id="taxForm">
            <div class="form-group">
                <label for="income1">
                    <span class="icon">💼</span>Primary Income Source (Annual)
                </label>
                <input 
                    type="number" 
                    id="income1" 
                    name="income1" 
                    step="0.01" 
                    min="0"
                    placeholder="Enter your primary income amount"
                    required
                >
                <div class="input-note">
                    Examples: Salary, wages, primary employment income
                </div>
            </div>
            
            <div class="form-group">
                <label for="income2">
                    <span class="icon">🏢</span>Secondary Income Source (Annual)
                </label>
                <input 
                    type="number" 
                    id="income2" 
                    name="income2" 
                    step="0.01" 
                    min="0"
                    placeholder="Enter your secondary income amount"
                    required
                >
                <div class="input-note">
                    Examples: Freelancing, rental income, investment returns, part-time work
                </div>
            </div>
            
            <button type="submit" class="submit-btn">
                <span class="icon">🧮</span>Calculate Total Income
            </button>
        </form>
        
        <div class="disclaimer">
            <div class="disclaimer-title">📝 Important Note:</div>
            This calculator provides an estimate of your total income from two sources. 
            Please consult with a qualified tax professional for comprehensive tax planning 
            and to understand all applicable deductions and tax implications.
        </div>
    </div>

    <script>
        // Client-side validation feedback
        document.getElementById('taxForm').addEventListener('submit', function(e) {
            const income1 = document.getElementById('income1').value;
            const income2 = document.getElementById('income2').value;
            
            if (!income1 || !income2) {
                e.preventDefault();
                alert('Please fill in both income fields.');
                return false;
            }
            
            if (parseFloat(income1) < 0 || parseFloat(income2) < 0) {
                e.preventDefault();
                alert('Income amounts cannot be negative.');
                return false;
            }
        });
        
        // Add currency formatting on blur
        function formatCurrency(input) {
            const value = parseFloat(input.value);
            if (!isNaN(value) && value >= 0) {
                // Basic formatting feedback
                input.style.background = '#e8f5e8';
                setTimeout(() => {
                    input.style.background = '#f8f9fa';
                }, 300);
            }
        }
        
        document.getElementById('income1').addEventListener('blur', function() {
            formatCurrency(this);
        });
        
        document.getElementById('income2').addEventListener('blur', function() {
            formatCurrency(this);
        });
    </script>
</body>
</html>